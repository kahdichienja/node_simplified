export class SetresultsDto {
    wins!: number;
    draws!: number;
    losses!: number;
    acive!: boolean;
    // wins, draws, losses, acive
  }
export class TeamDto {
    name!: string;
    flagUrl!: string;

    // name, flagUrl
  }